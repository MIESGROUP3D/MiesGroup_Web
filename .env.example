# Copiar a .env.local (no se sube a git)

# Subcarpeta donde vive el sitio. Vacío en local.
# En GitHub Pages lo define el workflow (.github/workflows/nextjs.yml) como /MiesGroup_Web.
NEXT_PUBLIC_BASE_PATH=

# El mockup es un sitio estático: el formulario de contacto solo simula el envío.
# Al pasar a un hosting con servidor se vuelve a agregar la ruta de API con Resend
# (RESEND_API_KEY, CONTACT_TO, CONTACT_FROM).
